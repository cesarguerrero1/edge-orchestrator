[project]
name = "edge-orchestrator"
version = "0.1.0"
description = "This project is just an exercise in system architecture of an edge orchestrator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "pyright>=1.1.408",
  "ruff>=0.15.8",
]
